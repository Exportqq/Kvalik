🌸 Учебное приложение "Магазин цветов" на Python
📖 Что это за программа?
Эта программа — простая система управления заказами для цветочного магазина. Она позволяет:

📦 Просматривать товары (цветы, аксессуары, упаковку)

👥 Входить под разными пользователями (админ или клиент)

🛒 Делать заказы (для клиентов)

📊 Смотреть статистику продаж (для админа)

✏️ Добавлять/удалять товары (для админа)

🎯 Для кого это обучение?
Для полных новичков в программировании! Объясняю максимально просто, как будто рассказываю другу.

🛠️ Что нужно для запуска?
Шаг 1: Установи Python
Зайди на сайт https://www.python.org

Нажми "Downloads" → скачай Python (бери версию 3.10 или новее)

Устанавливай, обязательно поставь галочку "Add Python to PATH"

Шаг 2: Скопируй код программы
Создай на компьютере папку, например, "Цветочный магазин"

Открой программу "Блокнот" (или любой текстовый редактор)

Скопируй весь код из файла с программой (который ты мне дал)

Сохрани файл в папку с названием магазин.py

Шаг 3: Запусти программу
Открой папку с файлом магазин.py

Щелкни правой кнопкой мыши по пустому месту в папке

Выбери "Открыть в командной строке" (или "Open in Terminal")

В черном окне напиши: python магазин.py и нажми Enter

👨‍💻 Как пользоваться программой?
1. Вход в систему
При запуске увидишь окно входа

Для админа: логин admin, пароль admin

Для клиента: логин user, пароль user

2. Если ты клиент (user):
✅ Можно:

Смотреть товары в каталоге

Делать заказы

Смотреть историю своих заказов

❌ Нельзя:

Добавлять новые товары

Удалять товары

Смотреть статистику продаж

3. Если ты админ (admin):
✅ Можно ВСЁ:

Все что может клиент

Плюс добавлять новые товары

Удалять товары

Видеть выручку за сегодня

Выходить из системы

📚 Как работает программа? (Объяснение для новичка)
Часть 1: База данных (SQLite)
text
# Это как "память" программы, где хранится вся информация
# Три главные таблицы (как три листа в Excel):

# 1. Пользователи (users)
#    | Логин | Пароль | Роль |
#    | admin | admin  | admin|

# 2. Товары (products)
#    | Название | Цена | Остаток | Категория |
#    | Цветы    | 350  | 10      | цветы     |

# 3. Заказы (orders)
#    | Кто купил | Что купил | Сколько | Сумма | Когда |
Часть 2: Окна и кнопки (Tkinter)
text
# Tkinter — это набор "кирпичиков" для создания окон:
# - Кнопки (Button)
# - Поля ввода (Entry)
# - Таблицы (Treeview)
# - Надписи (Label)
# - Окна сообщений (messagebox)

# Программа как дом:
# 1. Сначала строишь фундамент (окно)
# 2. Добавляешь стены (рамки - Frame)
# 3. Расставляешь мебель (кнопки, таблицы)
# 4. Подключаешь электричество (команды на кнопках)
Часть 3: Логика программы
text
1. Запуск программы
   ↓
2. Показ окна входа
   ↓
3. Проверка логина/пароля
   ↓
4. Показ главного окна (разное для админа и клиента)
   ↓
5. Ожидание действий пользователя
   ↓
6. Выполнение команд (заказ, добавление товара и т.д.)
   ↓
7. Сохранение в базу данных
🔍 Разбираем код по частям
1. Начало программы (импорты)
text
import tkinter as tk  # Библиотека для создания окон
from tkinter import ttk, messagebox, simpledialog  # Дополнительные элементы
import sqlite3  # Для работы с базой данных
from datetime import datetime, timedelta  # Для работы с датами
Простыми словами: "Я приношу в свою программу готовые наборы инструментов".

2. Класс DB (База данных)
text
class DB:
    def __init__(self):
        self.conn = sqlite3.connect("app.db")  # Подключаемся к файлу-базе
        self.cur = self.conn.cursor()  # Создаем "ручку" для записей
        self.setup()  # Настраиваем таблицы
Простыми словами: Это "секретарь" программы, который запоминает все данные.

3. Класс App (Главное приложение)
text
class App:
    def __init__(self, root):
        self.root = root  # Главное окно
        self.db = DB()  # Создаем "секретаря"
        self.user = None  # Пока никто не вошел
        self.login_screen()  # Показываем окно входа
Простыми словами: Это "директор" программы, который управляет всем.

4. Окно входа
text
def login_screen(self):
    self.clear()  # Очищаем экран
    box = ttk.Frame(self.root)  # Создаем рамку
    box.place(relx=0.5, rely=0.5, anchor="center")  # Ставим по центру
    
    # Создаем поле для логина
    self.u = ttk.Entry(card, width=30)
    self.u.insert(0, "admin")  # Пишем "admin" по умолчанию
Простыми словами: Создаем красивое окно посередине экрана с полями для ввода.

5. Проверка входа
text
def login(self):
    r = self.db.login(self.u.get(), self.p.get())  # Спрашиваем у "секретаря"
    if not r:
        messagebox.showerror("Ошибка", "Неверные данные")  # Показываем ошибку
        return
    self.user = r  # Запоминаем кто вошел
    self.dashboard()  # Показываем главное меню
Простыми словами: "Секретарь" проверяет в своей книжке, есть ли такой пользователь.

6. Главное окно (каталог товаров)
text
def dashboard(self):
    # Показываем заголовок
    ttk.Label(head, text="Каталог товаров").pack(side="left")
    
    # Показываем кто вошел
    ttk.Label(head, text=f"{self.user['username']}").pack(side="right")
    
    # Создаем таблицу товаров
    self.tree = ttk.Treeview(columns=("услуга", "Цена", "Остаток", "Категория"))
Простыми словами: Показываем красивую таблицу со всеми товарами.

7. Покупка товара
text
def buy(self):
    sel = self.tree.focus()  # Смотрим что выбрал пользователь
    name, price, stock, _ = self.tree.item(sel)["values"]  # Получаем данные
    
    # Спрашиваем сколько штук
    q = simpledialog.askinteger("Количество", name)
    
    # Сохраняем заказ
    self.db.add_order(self.user["username"], name, q, q * float(price))
Простыми словами: Запоминаем что купили, сколько и на какую сумму.

🚀 Как создать свою программу? (Пошагово)
Шаг 1: Начни с шаблона
text
import tkinter as tk
from tkinter import ttk

class MyApp:
    def __init__(self):
        self.window = tk.Tk()
        self.window.title("Моя первая программа")
        self.create_widgets()
        self.window.mainloop()
    
    def create_widgets(self):
        # Здесь создаем кнопки и надписи
        ttk.Button(self.window, text="Привет!", command=self.say_hello).pack()
    
    def say_hello(self):
        print("Привет мир!")

# Запуск программы
if __name__ == "__main__":
    app = MyApp()
Шаг 2: Добавь базу данных
text
import sqlite3

class Database:
    def __init__(self):
        self.conn = sqlite3.connect("my_db.db")
        self.create_tables()
    
    def create_tables(self):
        # Создай свою первую таблицу
        self.conn.execute("CREATE TABLE IF NOT EXISTS users (id INTEGER, name TEXT)")
Шаг 3: Соедини окна и базу данных
text
# Создай поле для ввода имени
name_entry = ttk.Entry(window)

# При нажатии кнопки "Сохранить"
def save_name():
    name = name_entry.get()  # Берем текст из поля
    database.save_to_db(name)  # Сохраняем в базу

ttk.Button(window, text="Сохранить", command=save_name).pack()
🧩 Основные понятия для запоминания
1. Класс - как чертеж машины
text
class Машина:
    def __init__(self):
        self.цвет = "красный"  # Характеристики
        self.скорость = 0      # Характеристики
    
    def ехать(self):  # Действия
        self.скорость = 60
2. Функция/Метод - как действие
text
def приветствовать(имя):  # Функция с параметром
    print(f"Привет, {имя}!")  # Что делает функция

приветствовать("Вася")  # Вызов функции
3. Переменная - как коробка для хранения
text
возраст = 25  # В коробку "возраст" положили число 25
имя = "Анна"  # В коробку "имя" положили текст
4. База данных - как библиотека
text
Полка "Книги"        Полка "Читатели"
[Гарри Поттер]       [Иванов Иван]
[Война и мир]        [Петров Петр]
5. Событие - как звонок телефона
text
# Когда нажимают кнопку (событие)
def когда_нажали_кнопку():  # Это "ответ на звонок"
    print("Кнопку нажали!")

# Связываем кнопку с функцией
кнопка = ttk.Button(окно, text="Нажми меня", command=когда_нажали_кнопку)
🏗️ Структура программы (как пазл)
text
┌─────────────────────────────────────────┐
│           ГЛАВНОЕ ОКНО (Tk)            │
├─────────────────────────────────────────┤
│  ┌──────────────────────────────────┐  │
│  │     РАМКА (Frame) - Вход         │  │
│  │  ┌───────────────┐               │  │
│  │  │   Логин: [___]│               │  │
│  │  │ Пароль: [___] │               │  │
│  │  │  [ ВОЙТИ ]    │               │  │
│  │  └───────────────┘               │  │
│  └──────────────────────────────────┘  │
├─────────────────────────────────────────┤
│  ┌──────────────────────────────────┐  │
│  │     РАМКА (Frame) - Каталог      │  │
│  │  ┌─────────────────────────────┐ │  │
│  │  │ Товар | Цена | Остаток      │ │  │
│  │  ├─────────────────────────────┤ │  │
│  │  │ Цветы | 350  | 10           │ │  │
│  │  └─────────────────────────────┘ │  │
│  └──────────────────────────────────┘  │
├─────────────────────────────────────────┤
│  ┌──────────────────────────────────┐  │
│  │     РАМКА (Frame) - Кнопки       │  │
│  │  [Купить] [История] [Выход]      │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
🔧 Частые проблемы и решения
Проблема 1: "Python не найден"
Решение: При установке Python обязательно поставь галочку "Add Python to PATH"

Проблема 2: "Ошибка в коде"
Решение:

Скопируй код точно как есть

Проверь все отступы (в Python это важно!)

Убери лишние пробелы в начале строк

Проблема 3: "Окно сразу закрывается"
Решение: Добавь в конец программы:

text
input("Нажми Enter для выхода...")
Проблема 4: "Не вижу русские буквы"
Решение: Сохрани файл в кодировке UTF-8

Проблема 5: "Ошибка 'No module named tkinter'"
Решение: При установке Python выбери "Install all features"

📈 Что можно улучшить в программе?
Улучшение 1: Добавить фотографии товаров
text
from PIL import Image, ImageTk  # Нужно установить: pip install pillow

# Загружаем картинку
photo = Image.open("цветы.jpg")
photo_tk = ImageTk.PhotoImage(photo)
ttk.Label(window, image=photo_tk).pack()
Улучшение 2: Сделать скидки
text
def calculate_price(price, quantity, discount=0):
    total = price * quantity
    if discount > 0:
        total = total * (1 - discount/100)
    return total
Улучшение 3: Добавить корзину
text
class Cart:
    def __init__(self):
        self.items = []  # Список товаров в корзине
    
    def add_item(self, name, price, quantity):
        self.items.append({
            "name": name,
            "price": price,
            "quantity": quantity
        })
🎓 Следующие шаги в обучении
1. Измени дизайн
Поменяй цвета (bg="#цвет")

Добавь шрифты (font=("Arial", 12))

Сделай картинки для кнопок

2. Добавь новые функции
Поиск товаров

Сортировка по цене

Печать чека

Отправка email-уведомлений

3. Сделай свою тему
Магазин одежды

Кафе/ресторан

Библиотека

Кинотеатр

📚 Полезные ресурсы
Для начинающих:
Python для детей - книга, очень просто объясняет

Stepik.org - бесплатные курсы на русском

YouTube канал "Python на практике" - много видеоуроков

Для практики:
Измени названия товаров в коде

Добавь нового пользователя

Создай свою категорию товаров

Измени цвета интерфейса

❓ Частые вопросы
Вопрос 1: Нужно ли платить за Python?
Ответ: Нет, Python полностью бесплатный!

Вопрос 2: Сколько времени нужно, чтобы научиться?
Ответ: Основы можно понять за неделю, чтобы писать свои программы — 1-2 месяца.

Вопрос 3: Сложно ли учить программирование?
Ответ: Как учиться кататься на велосипеде: сначала страшно, потом привыкаешь, потом получаешь удовольствие!

Вопрос 4: Что делать если ничего не понимаю?
Ответ:

Не переживай — все через это проходили

Меняй по одному параметру и смотри что происходит

Задавай вопросы в чатах для начинающих

💪 Попробуй сам!
Задание 1: Измени приветствие
Найди в коде строчку с текстом "Каталог товаров" и поменяй на "Мой цветочный магазин"

Задание 2: Добавь новый товар
В функции setup добавь новую строку:

text
self.cur.execute("INSERT OR IGNORE INTO products VALUES(NULL,'букет',500,15,'цветы')")
Задание 3: Создай нового пользователя
Добавь после admin и user:

text
self.cur.execute("INSERT OR IGNORE INTO users VALUES(NULL,'мария','123','user')")
🎉 Поздравляю!
Ты только что:

✅ Узнал что такое программирование

✅ Понял как работают базы данных

✅ Увидел как создаются окна и кнопки

✅ Запустил свою первую серьезную программу

Ты на правильном пути! Каждая большая программа начинается с маленьких шагов. Продолжай экспериментировать, задавать вопросы и пробовать новое!

📞 Нужна помощь?
Если что-то не работает или не понятно:

Перечитай соответствующую часть этого README

Попробуй поискать ответ в интернете

Спроси у знакомых кто разбирается в программировании

Помни: каждая ошибка — это шаг к успеху!

Удачи в изучении программирования! 🚀

Создано с ❤️ для начинающих программистов
